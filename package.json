{
  "name": "akshar-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "python3 scripts/check.py",
    "hooks:install": "git config --local core.hooksPath .githooks",
    "content-server": "python3 -m http.server 8787"
  }
}
